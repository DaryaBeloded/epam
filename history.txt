      cd e:
      ls
      cd epam
      git clone https://github.com/DaryaBeloded/epam.git
      ls
      cd epam
      cd ../
      cd ~/.ssh
      git clone https://github.com/DaryaBeloded/epam.git
      cd epam
      echo init message >> README.md
      git add README.md
      git cat README.md
      git cat -README.md
      cat README.md
      git commit -m "first commit in master"
      echo blabla >> README.md
      git add README.md
      git commit -m "second commit in master"
      echo hello world! >> file.txt
      git add file.txt
      git commit -m "third commit in master"
      git branch dev
      gir checkout dev
      git checkout dev
      echo changing file.txt from dev >> file.txt
      git add file.txt
      git commit -m "first commit in dev"
      echo changing README.md from dev >> README.md
      git add README.md
      git commit -m "secon commit in dev"
      git checkout master
      git merge dev
      git push --all
      echo string for rebase with conflict >> README.md
      git add README.md
      git commit -m "commit from master for rebase with conflict"
      git checkout dev
      echo first commit in dev for rebase >> file.txt
      git add file.txt
      git commit -m "first commit in dev for rebase"
      echo string for rebase >> README.md
      git add README.md
      git commit -m "another commit for rebase from dev"
      git rebase master
      vim README.md
      git add README.md
      git rebase --continue
      git push --all
      git checkout master
      git merge dev
      git checkout dev
      echo string for cherry-pick >> file.txt
      git add file.txt
      git commit -m "first commit for cherry-pick"
      echo string for revert >> README.md
      git add README.md
      git commit -m "commit for revert"
      git log
      git checkout master
      git cherry-pick 163c288cd0439e0a76c1d972ec3a15da62d388a8
      git checkout dev
      git log
      git revert ae95312d0f463b89538e7fa6110a8ab0cccd1b2c
      git push --all
      history > history.txt
